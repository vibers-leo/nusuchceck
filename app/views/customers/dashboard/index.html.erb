<div class="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50 py-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- 헤더 -->
    <div class="mb-8 flex items-start justify-between">
      <div>
        <h1 class="text-3xl font-bold text-gray-900 mb-2">
          환영합니다, <%= current_user.name || current_user.email %>님! 👋
        </h1>
        <p class="text-gray-600">누수체크 고객 대시보드</p>
      </div>
      <%= link_to customers_profile_path, class: "flex items-center gap-2 px-4 py-2 rounded-lg bg-white border border-gray-200 text-gray-700 hover:bg-gray-50 hover:border-gray-300 transition" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
        </svg>
        내 프로필
      <% end %>
    </div>

    <!-- 통계 카드 -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
      <!-- 진행 중인 체크 -->
      <div class="bg-white rounded-xl shadow-sm p-6 border-l-4 border-blue-500">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm text-gray-600 mb-1">진행 중인 체크</p>
            <p class="text-3xl font-bold text-gray-900"><%= @active_requests.count %></p>
          </div>
          <div class="p-3 bg-blue-100 rounded-lg">
            <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
            </svg>
          </div>
        </div>
      </div>

      <!-- 견적 대기 -->
      <div class="bg-white rounded-xl shadow-sm p-6 border-l-4 border-yellow-500">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm text-gray-600 mb-1">견적 대기 중</p>
            <p class="text-3xl font-bold text-gray-900"><%= @pending_estimates.count %></p>
          </div>
          <div class="p-3 bg-yellow-100 rounded-lg">
            <svg class="w-8 h-8 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>
      </div>

      <!-- 이번 달 체크 -->
      <div class="bg-white rounded-xl shadow-sm p-6 border-l-4 border-green-500">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm text-gray-600 mb-1">이번 달 체크</p>
            <p class="text-3xl font-bold text-gray-900"><%= @monthly_requests_count %></p>
          </div>
          <div class="p-3 bg-green-100 rounded-lg">
            <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>
      </div>

      <!-- 이번 달 지출 -->
      <div class="bg-white rounded-xl shadow-sm p-6 border-l-4 border-purple-500">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm text-gray-600 mb-1">이번 달 지출</p>
            <p class="text-2xl font-bold text-gray-900"><%= number_to_currency(@monthly_total_spent, unit: "₩", precision: 0) %></p>
          </div>
          <div class="p-3 bg-purple-100 rounded-lg">
            <svg class="w-8 h-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 9V7a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2m2 4h10a2 2 0 002-2v-6a2 2 0 00-2-2H9a2 2 0 00-2 2v6a2 2 0 002 2zm7-5a2 2 0 11-4 0 2 2 0 014 0z"></path>
            </svg>
          </div>
        </div>
      </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
      <!-- 왼쪽: 진행 중인 체크 -->
      <div class="lg:col-span-2 space-y-6">
        <!-- 진행 중인 체크 -->
        <div class="bg-white rounded-xl shadow-sm p-6">
          <div class="flex items-center justify-between mb-6">
            <h2 class="text-xl font-bold text-gray-900">진행 중인 체크</h2>
            <%= link_to "전체 보기 →", customers_requests_path, class: "text-blue-600 hover:text-blue-700 text-sm font-medium" %>
          </div>

          <% if @active_requests.any? %>
            <div class="space-y-4">
              <% @active_requests.each do |request| %>
                <div class="border border-gray-200 rounded-lg p-4 hover:shadow-md transition-shadow">
                  <div class="flex items-start justify-between">
                    <div class="flex-1">
                      <div class="flex items-center gap-2 mb-2">
                        <span class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-xs font-medium">
                          <%= request.status_label %>
                        </span>
                        <span class="text-sm text-gray-500">
                          <%= time_ago_in_words(request.created_at) %> 전
                        </span>
                      </div>
                      <h3 class="font-semibold text-gray-900 mb-1">
                        <%= request.symptom_type_label %> - <%= request.building_type_label %>
                      </h3>
                      <p class="text-sm text-gray-600 mb-2">
                        <%= request.address %>
                      </p>
                      <% if request.master %>
                        <p class="text-sm text-gray-500">
                          담당 전문가: <%= request.master.name || request.master.email %>
                        </p>
                      <% end %>
                    </div>
                    <%= link_to "상세보기", customers_request_path(request), class: "px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors text-sm font-medium" %>
                  </div>
                </div>
              <% end %>
            </div>
          <% else %>
            <div class="text-center py-12">
              <svg class="w-16 h-16 text-gray-300 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
              <p class="text-gray-500 mb-4">진행 중인 체크가 없습니다</p>
              <%= link_to "새 체크 접수하기", new_customers_request_path, class: "inline-flex items-center px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors font-medium" %>
            </div>
          <% end %>
        </div>

        <!-- 최근 완료된 체크 -->
        <% if @recent_completed.any? %>
          <div class="bg-white rounded-xl shadow-sm p-6">
            <h2 class="text-xl font-bold text-gray-900 mb-6">최근 완료된 체크</h2>
            <div class="space-y-4">
              <% @recent_completed.each do |request| %>
                <div class="border border-gray-200 rounded-lg p-4">
                  <div class="flex items-start justify-between">
                    <div class="flex-1">
                      <div class="flex items-center gap-2 mb-2">
                        <span class="px-3 py-1 bg-green-100 text-green-700 rounded-full text-xs font-medium">
                          완료
                        </span>
                        <span class="text-sm text-gray-500">
                          <%= request.closed_at.strftime("%Y-%m-%d") %>
                        </span>
                      </div>
                      <h3 class="font-semibold text-gray-900 mb-1">
                        <%= request.symptom_type_label %> - <%= request.building_type_label %>
                      </h3>
                      <p class="text-sm text-gray-600">
                        <%= request.address %>
                      </p>
                    </div>
                    <div class="flex gap-2">
                      <%= link_to "상세보기", customers_request_path(request), class: "px-4 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors text-sm font-medium" %>
                      <% if request.can_be_reviewed? %>
                        <%= link_to "리뷰 작성", new_customers_request_review_path(request), class: "px-4 py-2 bg-yellow-500 text-white rounded-lg hover:bg-yellow-600 transition-colors text-sm font-medium" %>
                      <% end %>
                    </div>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>

      <!-- 오른쪽: 빠른 액션 & 알림 -->
      <div class="space-y-6">
        <!-- 빠른 액션 -->
        <div class="bg-gradient-to-br from-blue-600 to-blue-700 rounded-xl shadow-lg p-6 text-white">
          <h3 class="text-lg font-bold mb-4">빠른 액션</h3>
          <div class="space-y-3">
            <%= link_to new_customers_request_path, class: "flex items-center gap-3 bg-white bg-opacity-20 hover:bg-opacity-30 rounded-lg p-3 transition-all" do %>
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
              </svg>
              <span class="font-medium">새 체크 접수</span>
            <% end %>

            <%= link_to customers_requests_path, class: "flex items-center gap-3 bg-white bg-opacity-20 hover:bg-opacity-30 rounded-lg p-3 transition-all" do %>
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
              </svg>
              <span class="font-medium">내 체크 목록</span>
            <% end %>
          </div>
        </div>

        <!-- 최근 알림 -->
        <% if @recent_notifications.any? %>
          <div class="bg-white rounded-xl shadow-sm p-6">
            <h3 class="text-lg font-bold text-gray-900 mb-4">최근 알림</h3>
            <div class="space-y-3">
              <% @recent_notifications.each do |notification| %>
                <div class="flex gap-3 p-3 <%= notification.read? ? 'bg-gray-50' : 'bg-blue-50' %> rounded-lg">
                  <div class="flex-shrink-0">
                    <div class="w-2 h-2 bg-blue-600 rounded-full mt-2"></div>
                  </div>
                  <div class="flex-1">
                    <p class="text-sm text-gray-900 font-medium"><%= notification.title %></p>
                    <p class="text-xs text-gray-600 mt-1"><%= notification.message %></p>
                    <p class="text-xs text-gray-400 mt-1"><%= time_ago_in_words(notification.created_at) %> 전</p>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>

        <!-- 도움말 -->
        <div class="bg-purple-50 border border-purple-200 rounded-xl p-6">
          <h3 class="text-lg font-bold text-purple-900 mb-3">💡 이용 가이드</h3>
          <ul class="space-y-2 text-sm text-purple-800">
            <li class="flex items-start gap-2">
              <span class="text-purple-600">•</span>
              <span>체크 접수 시 사진이나 동영상을 첨부하면 더 정확한 진단이 가능합니다</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-purple-600">•</span>
              <span>견적서는 접수 후 24시간 이내에 제공됩니다</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-purple-600">•</span>
              <span>공사 완료 후 리뷰를 작성하면 다른 고객에게 도움이 됩니다</span>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
